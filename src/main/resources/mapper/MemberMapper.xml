<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jpabook.mgpractice.mapper.MemberMapper">

    <insert id="insertMember" useGeneratedKeys="true" keyProperty="memberId" keyColumn="member_id">
        INSERT INTO member (username, password, name, role)
        VALUES (#{username}, #{password}, #{name}, #{role})
    </insert>

    <select id="findByUsername" resultType="jpabook.mgpractice.domain.Member">
        SELECT member_id, username, password, name, role, created_at
        FROM member
        WHERE username = #{username}
    </select>

    <update id="updateMember" parameterType="jpabook.mgpractice.domain.Member">
        UPDATE MEMBER
        <set>
            <if test="password != null and password != ''">
                PASSWORD = #{password},
            </if>
            <if test="name != null and name != ''">
                NAME = #{name}
            </if>
        </set>
        WHERE USERNAME = #{username}
    </update>

</mapper>

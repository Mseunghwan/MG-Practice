spring.application.name=MG-Practice

# 1. DB Connection
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
# ?? ????? ?? ??(?? ??) localhost? ?????.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:oracle:thin:@localhost:1521/XEPDB1}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:mg_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:1234}

# 2. MyBatis ??
mybatis.mapper-locations=classpath:mapper/**/*.xml
mybatis.configuration.map-underscore-to-camel-case=true
mybatis.type-aliases-package=jpabook.mgpractice.domain

# 3. ?? ??
logging.level.jpabook.mgpractice.mapper=DEBUG